{
	"name": "hindsight-contact-form-handler",
	"version": "1.0.0",
	"description": "Lambda function to handle contact form submissions via AWS SES",
	"main": "hindsight-contact-form-handler.js",
	"dependencies": {
		"aws-sdk": "^2.1692.0"
	},
	"scripts": {
		"package": "zip -r function.zip ."
	}
}
